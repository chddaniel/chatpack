---
"@chatpack/core": patch
---

Docs: hybrid auth recipe (bearer for REST, cookie for `/stream`), explicit HTTP-envelope vs bare `chat.api.*` return-shape clarification, and `getConversation` throw behavior called out (README + TSDoc).
