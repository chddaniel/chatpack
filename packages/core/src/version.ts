/**
 * The `@chatpack/core` package version.
 *
 * Kept as a plain constant (rather than reading package.json at runtime) so
 * it works identically in every runtime and bundler. Updated as part of the
 * release process.
 *
 * @module
 */

/** Current `@chatpack/core` version. */
export const VERSION = "0.1.0";
